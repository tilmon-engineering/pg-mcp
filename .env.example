# Local development credentials for the disposable PostgreSQL fixture.
# Copy to .env; mise loads it automatically for project tasks.
PG_MCP_DSN=postgresql://postgres:postgres@127.0.0.1:55432/postgres
